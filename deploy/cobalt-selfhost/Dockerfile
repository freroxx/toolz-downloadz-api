# For hosts that build from a GitHub repo (Back4App, etc.)
# Set API_URL env in your host's dashboard to your final public URL AFTER first deploy.
FROM ghcr.io/imputnet/cobalt:11
ENV API_PORT=9000
EXPOSE 9000
